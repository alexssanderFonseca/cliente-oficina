apiVersion: v1
kind: Service
metadata:
  name: cliente-cliente-app-service
  namespace: oficina-ns
  labels:
    app: cliente-app
spec:
  type: ClusterIP
  selector:
    app: cliente-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9091
